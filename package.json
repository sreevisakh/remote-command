{
  "name": "remote-command",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd src && babel **/*.js --out-dir ../lib/ --sourcemaps",
    "watch": "cd src && babel --watch **/*.js --out-dir ../lib/ --sourcemaps"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.1",
    "chalk": "^1.1.3",
    "ejs": "^2.5.6",
    "express": "^4.15.2",
    "lodash": "^4.17.4",
    "request": "^2.81.0",
    "socket.io": "^1.7.3",
    "socket.io-client": "^1.7.3",
    "uuid": "^3.0.1"
  },
  "devDependencies": {
    "mocha": "^3.2.0"
  }
}
